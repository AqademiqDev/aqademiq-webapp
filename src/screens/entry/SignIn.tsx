import Welcome from './Welcome';

/* Route /signin — README §4.2 maps it to frame 00.2's right pane, i.e. the
   same Welcome screen with the sign-in form. */

export default function SignIn() {
  return <Welcome />;
}
